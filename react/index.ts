/*
import { useState as _useState } from "react";

const isServer = (typeof window === "undefined") as false;
console.log(_useState);

export const useState = isServer
  ? (defautValue: any) => [defautValue, () => {}]
  : _useState;
*/
