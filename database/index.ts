("<Bunext_TypeImposts>");
("<Bunext_TypeImposts>");

export async function Database() {
  if (typeof window !== "undefined") return {};
  const Table = (await import("./class")).Table;
  ("<Bunext_DBExport>");
  ("<Bunext_DBExport>");
}
