import { PreLoadPath } from "./index.tsx";

/**
 * Preload path loading the Javascript reducing loading time
 * @param path the path to preload
 */
const preLoad = PreLoadPath;

export default preLoad;
