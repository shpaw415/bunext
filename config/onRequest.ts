// bypass the server Response and return a custom response or
// return void otherwise

import type { OnRequestType } from "bunext-js/types";

const onRequest: OnRequestType = async (request) => { };

export default onRequest;
