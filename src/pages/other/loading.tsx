

export default function Loading() {
    return (
        <div style={{ padding: 20 }}>
            <h2>Loading...</h2>
            <p>This is the fallback loading component.</p>
        </div>
    );
}