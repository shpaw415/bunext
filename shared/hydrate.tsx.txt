// hydrate.ts
/// <reference lib="dom" />

import <DEFAULT> from "<PATH>";
import { hydrateRoot } from "react-dom/client";
import init from "bunext/shared/shared";
import React from "react";
const App = require("bunext/src/server-response").App as ({children}: {children?: JSX.Element | string}) => JSX.Element;

init();

hydrateRoot(document, App({children: <DEFAULT>()}));